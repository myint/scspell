language: python

python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"

before_install:
    - pip install --quiet git+https://github.com/myint/cram.git
    - cram --indent=4 test.t

install:
    - python setup.py --quiet install

script:
    - cram --indent=4 test.t
